{
    "$schema": "https://railway.app/railway.schema.json",
    "deploy": {
        "startCommand": "python -u manage.py migrate --noinput && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --log-level debug --access-logfile -",
        "healthcheckPath": "/",
        "healthcheckTimeout": 300,
        "restartPolicyType": "ON_FAILURE"
    }
}